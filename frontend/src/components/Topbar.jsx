function Topbar() {
  return (
    <header className="topbar">
      <div>
        <p>Overview</p>
        <h1>Dashboard</h1>
      </div>

      <button type="button">Add Appointment</button>
    </header>
  );
}

export default Topbar;
